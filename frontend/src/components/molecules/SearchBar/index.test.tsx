import { screen, render } from "@testing-library/react";
import "@testing-library/jest-dom";
import React from "react";
import SearchBar from ".";

test("Seaarh Bar test", () => {
    render(<SearchBar />)
})